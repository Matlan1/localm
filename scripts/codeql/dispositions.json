{
  "_about": [
    "Adjudication of every open py/path-injection alert on this repo, keyed on",
    "(file :: enclosing function) rather than alert number or line, because both of",
    "those move whenever a fix edits a sink line and a dismissal keyed on either",
    "silently detaches. See sweep_dismissals.py for what that failure looks like.",
    "",
    "Each key's text is the dismissal justification posted to the alert. It has to",
    "be true of EVERY alert in that function, so a function holding sinks of two",
    "kinds gets a comment covering both rather than the tidier of the two labels.",
    "",
    "Provenance: the bulk of this table restates an INDEPENDENT reader's per-alert",
    "adjudication (they had the SARIF, the tree, and no access to the triage",
    "reasoning). The entries for the 30 alerts their list omitted were derived here",
    "from each alert's own code flows. Nothing in this file is classified by",
    "resemblance to a neighbouring alert."
  ],
  "justifications": {
    "registry-path-chain": "False positive. Taint originates in localm's own registry.json and reaches a read-only probe of a model file (exists / is_file / is_dir / stat, or a bounded metadata read). Every reported flow passes through registry._entry_path, which refuses a non-dict entry, a null, non-string or empty path, and any '..' component under both Windows and POSIX path flavours; and through get_model_info(..., allow_direct_path=False), which refuses an unregistered caller-supplied path and is left at that default by every call site on every reported flow. CodeQL models neither guard: _entry_path returns the original value rather than a transformed one, and the allow_direct_path test is on a different parameter from the tainted one.",
    "plugin-id-choke": "False positive. The plugin id is validated by _is_valid_plugin_name (directly, or through _check_plugin_name) at every point where it becomes a path component: the _installed_dir / _store_dir choke points, and the staging assertion in _swap_in_store_copy, which additionally requires dest, tmp and backup all to sit in the installed root and raises otherwise. The id must equal its own basename and be identifier-shaped once '-' is folded to '_', so no separator, drive letter or '..' survives. CodeQL has no sanitizer model for str.isidentifier() or for name == Path(name).name, so it traces the value straight through the check.",
    "inline-dominating": "False positive. The guard is inline in the sink's own function and dominates it: it runs first on every path, with no branch around it, and raises. There is no cross-function traversal for a taint-tracking barrier to observe, so the query reports the sink without seeing the check immediately above it. Dominance here was established by reading the function, which is the only way to establish it: none of this repo's CodeQL tooling checks it.",
    "choke-point": "False positive, and structurally unclosable. This alert IS the confinement helper: the flagged expression is the path construction that the containment test needs in order to test containment. The construction therefore always precedes the check, so the guard can never appear as a barrier ahead of its own sink. Adding a further guard here would only introduce one more path expression on the same value, which is one more sink.",
    "confined-under": "False positive. Every flow passes through pathsafe.confined_under, which rejects an empty result, an absolute component, a drive-qualified component (checked per component, not only the first), any '..' component, and any result whose resolved location is not strictly below the base directory. Confirmed rather than argued: declaring confined_under a models-as-data barrier and re-running the query offline clears exactly these alerts.",
    "authorization-gated": "Not sanitized, and deliberately so. The destination is unconstrained because naming it is the feature: a user-picked export folder, a chosen third-party plugin source directory, an operator-configured endpoint. What stands in front of it is authorization, not confinement, so confining the destination would remove the feature. Recorded as accepted by design rather than mislabelled as barriered.\n\nTHE POSTURE THIS DEPENDS ON, stated because the disposition rests on it rather than on a guard: under a configured key the route requires a privileged scope (ADMIN / PLUGINS_ADMIN / fs_access=host), whose holder can already reach the same files by other means. In KEYLESS open mode there is no credential to check and the gates answer owner-equivalent: effective_fs_access returns 'host', and caller_scopes returns None, which the routes read as the owner.\n\nThat open mode is BY DESIGN AND CONFINED TO LOOPBACK, and the confinement is enforced rather than merely intended: binding past loopback without auth is fail-closed, the CLI refusing to start with exit code 2 (plugins/gui/cli.py; `localm serve` delegates to that same guard rather than a parallel one, and there is no config key for the bind host, so the CLI is the single chokepoint). Reaching it therefore requires an operator explicitly passing --insecure. The residual is a deliberate operator override, NOT an ambient hole - but it is still the one configuration in which 'no untrusted principal reaches this sink' stops holding, which is why it is named here instead of left implied.",
    "operator-env": "Accepted by threat model, not closed by a barrier, and filed that way on purpose: calling this barriered would hide a real finding behind a plausible label. The value is LLAMA_CPP_LIB, read from localm's own process environment, whose documented purpose is to name the llama.cpp shared library to load. Only the principal that launches the process can set it, and that principal can already execute code as the same OS user, so no boundary is crossed. There is no barrier here, and a barrier would be incoherent.",
    "guarded-delete-model": "False positive. This function holds sinks of two kinds and the comment covers both. The deletion is gated by is_owned_model_path(target), inline and dominating: it runs on the resolved path immediately before rmtree / unlink, with no branch around it, and the surrounding code resolves once and acts on that same resolved value so the decision and the action cannot refer to different files. The path itself arrives from registry.json through _entry_path (see the registry-path-chain note). CodeQL models neither guard.",
    "guarded-delete-plugin": "False positive. This function holds sinks of THREE kinds and the comment must cover all of them: the containment test itself (d.resolve().parent == root.resolve()) is a path expression on the tainted value and so is reported as a sink; it dominates the is_dir()/rmtree() delete pair below it; and it equally dominates the two is_dir() outcome-probe calls added after the delete attempt (querying whether removal actually succeeded, so the caller can report a real outcome instead of a bare success). All three are unreachable unless d already resolved to a direct child of the installed root - the raise on containment failure runs before any of them. Deliberately confining by resolved parent rather than by re-validating the id, because an already-installed plugin whose name is not identifier-shaped must still be uninstallable. NOTE (2026-07-29): the prior version of this text described the sinks as only the containment test and the rmtree, and singled out the containment test as the one flagged - that was never true of any specific alert (the analyser never flags its own guard as the sink it guards) and became untenable once the outcome-probe alerts (#252/#253) landed. Do not name a specific line in the SHORT text below; state the invariant instead, since which of the three shapes a given alert number lands on varies.",
    "model-lookup-gate": "False positive. This function holds sinks of two kinds and the comment covers both. The registry branch routes the stored path through _entry_path; the direct-path branch is reachable only when the caller passes allow_direct_path=True, and the check dominating it is inline in this function. Every call site on every reported flow leaves that parameter at its refusing default. CodeQL cannot model either: the guard tests a different parameter from the tainted one, and it has no boolean-literal context sensitivity.",
    "plugin-swap-invariant": "False positive. This function holds sinks of two kinds and the comment covers both. The plugin id was validated at the _installed_dir / _store_dir choke point before reaching here, and the function re-asserts the containment invariant inline, dominating all of its own sinks. CodeQL has no sanitizer model for the identifier-shaped id check, and no traversal to observe for the inline re-assertion.",
    "confinement-fixed-2026-07-29": "The check-then-use gap here was fixed on 2026-07-29: the sink now consumes the value the guard returned instead of the caller's original. The alert persists because the flagged expression is the path construction that the check necessarily follows (see the choke-point note), not because the flow is unguarded.",
    "weak-hash-generated-token": "False positive. The only human-chosen secret this system holds is the owner key, and it is routed to scrypt: _owner_identity derives it, and its keystore record declares the KDF construction, so the fast digest is never the value that authenticates it. Every other key reaching this hash is either a keystore token, which create_key generates as secrets.token_urlsafe(32) and is therefore 256 bits of CSPRNG output where no dictionary or rainbow table applies and hash speed is irrelevant, or a failed guess, which is hashed and discarded. The remaining traced flow is the migration helper _legacy_owner_identity, whose output is never persisted and never authenticates anything: it exists to FIND rows still carrying the old identity so they can be rewritten to the derived one.",
    "dev-cli-argv": "Not sanitized, and deliberately so: naming which coverage.json to check IS the feature, documented in the module's own docstring ('python scripts/check_coverage_floors.py [path/to/coverage.json]'). scripts/check_coverage_floors.py is dev-only tooling (release-excluded in release-manifest.toml, never ships); its only two callers are CI's own workflow step, which passes no argument at all (falls back to REPO/coverage.json), and a developer running it locally by hand, supplying the path themselves. Only the principal that invokes the script can set argv, and that principal is already running arbitrary code as the local OS user - there is no remote or lower-privileged actor this could expose a file to. Confining the argument to a fixed root would also break the documented workflow of pointing the script at a coverage.json produced anywhere on disk (used during this feature's own verification, at a scratch path outside the repository)."
  },
  "dispositions": {
    "localm/cli/maintenance.py::setup_embeddings": "registry-path-chain",
    "localm/inference/backends/gguf.py::__init__": "registry-path-chain",
    "localm/inference/backends/hf.py::__init__": "registry-path-chain",
    "localm/inference/backends/hf.py::_declares_custom_code": "registry-path-chain",
    "localm/inference/backends/llamacpp/_loader.py::_add_to_search_path": "operator-env",
    "localm/inference/backends/llamacpp/_loader.py::_force_vulkan_dedicated_vram": "operator-env",
    "localm/inference/backends/llamacpp/_loader.py::_register_ggml_backends": "operator-env",
    "localm/inference/backends/llamacpp/_loader.py::load_lib": "operator-env",
    "localm/inference/backends/llamacpp/_sizing.py::_model_bytes": "registry-path-chain",
    "localm/inference/backends/llamacpp/_sizing.py::embedder_ctx_reservation_bytes": "registry-path-chain",
    "localm/inference/embedder.py::_choose_embedder_gpu_layers": "registry-path-chain",
    "localm/inference/embedder.py::_maybe_swap_for_embedder": "registry-path-chain",
    "localm/inference/embedder.py::_preflight_vram": "registry-path-chain",
    "localm/inference/embedder.py::resolve_embedding_model_path": "registry-path-chain",
    "localm/inference/engine.py::_has_hf_model_artifacts": "registry-path-chain",
    "localm/inference/engine.py::_is_gguf": "registry-path-chain",
    "localm/inference/engine.py::_is_hf_dir": "registry-path-chain",
    "localm/inference/engine.py::model_display_name": "registry-path-chain",
    "localm/inference/http_server.py::_model_file_size": "registry-path-chain",
    "localm/inference/http_server.py::_unload_embedder_if_matches": "registry-path-chain",
    "localm/inference/residency.py::model_footprint_bytes": "registry-path-chain",
    "localm/media/comfy_client.py::contain_comfy_artifacts": "confined-under",
    "localm/media/paths.py::check_input_image": "choke-point",
    "localm/media/paths.py::confined_move_dest": "choke-point",
    "localm/model_manager/gguf.py::gguf_kv_bytes_per_token": "registry-path-chain",
    "localm/model_manager/pull.py::_is_same_repo": "registry-path-chain",
    "localm/model_manager/pull.py::_pull_url": "confinement-fixed-2026-07-29",
    "localm/model_manager/registry.py::_hf_is_vision": "registry-path-chain",
    "localm/model_manager/registry.py::_is_missing": "registry-path-chain",
    "localm/model_manager/registry.py::_reconcile": "registry-path-chain",
    "localm/model_manager/registry.py::_register": "registry-path-chain",
    "localm/model_manager/registry.py::_resolve_ollama_manifest": "registry-path-chain",
    "localm/model_manager/registry.py::find_aliases_by_path": "registry-path-chain",
    "localm/model_manager/registry.py::find_by_size": "registry-path-chain",
    "localm/model_manager/registry.py::find_sibling_mmproj": "registry-path-chain",
    "localm/model_manager/registry.py::get_model_info": "model-lookup-gate",
    "localm/model_manager/registry.py::get_model_mmproj": "confinement-fixed-2026-07-29",
    "localm/model_manager/registry.py::is_external_path": "choke-point",
    "localm/model_manager/registry.py::is_owned_model_path": "choke-point",
    "localm/model_manager/registry.py::list_models": "registry-path-chain",
    "localm/model_manager/registry.py::remove_model": "guarded-delete-model",
    "localm/model_manager/registry.py::sync_models_dir": "registry-path-chain",
    "localm/model_manager/registry.py::vision_capable_models": "registry-path-chain",
    "localm/model_manager/scan.py::_existing_registered_paths": "registry-path-chain",
    "localm/model_meta.py::_model_key": "registry-path-chain",
    "localm/pathsafe.py::confined_under": "choke-point",
    "localm/plugins/builtin/rag/plug.py::rag_add": "confinement-fixed-2026-07-29",
    "localm/plugins/builtin/rag/plug.py::rag_embedding_status": "authorization-gated",
    "localm/plugins/engine.py::_copy_third_party_source": "authorization-gated",
    "localm/plugins/engine.py::_dir_content_hash": "authorization-gated",
    "localm/plugins/engine.py::_maybe_refresh_builtin": "plugin-id-choke",
    "localm/plugins/engine.py::_provision_from_store": "plugin-id-choke",
    "localm/plugins/engine.py::_read_marker": "plugin-id-choke",
    "localm/plugins/engine.py::_reject_source_links": "authorization-gated",
    "localm/plugins/engine.py::_remove_installed_dir": "guarded-delete-plugin",
    "localm/plugins/engine.py::_store_dir": "inline-dominating",
    "localm/plugins/engine.py::_swap_in_store_copy": "plugin-swap-invariant",
    "localm/plugins/engine.py::_write_marker": "plugin-id-choke",
    "localm/plugins/engine.py::refresh": "plugin-id-choke",
    "localm/plugins/gui/routes/admin.py::create_comfy_launcher": "choke-point",
    "localm/plugins/gui/routes/admin.py::export_logs": "authorization-gated",
    "localm/plugins/gui/routes/admin.py::fs_dirs": "authorization-gated",
    "localm/plugins/mcpserver/server.py::list_models": "registry-path-chain",
    "scripts/report_issue.py::opener": "authorization-gated",
    "localm/auth.py::_fast_digest": "weak-hash-generated-token",
    "scripts/check_coverage_floors.py::main": "dev-cli-argv"
  },
  "reasons": {
    "registry-path-chain": "false positive",
    "plugin-id-choke": "false positive",
    "inline-dominating": "false positive",
    "choke-point": "false positive",
    "confined-under": "false positive",
    "authorization-gated": "won't fix",
    "operator-env": "won't fix",
    "guarded-delete-model": "false positive",
    "guarded-delete-plugin": "false positive",
    "model-lookup-gate": "false positive",
    "plugin-swap-invariant": "false positive",
    "confinement-fixed-2026-07-29": "false positive",
    "weak-hash-generated-token": "false positive",
    "dev-cli-argv": "won't fix"
  },
  "_limit": "GitHub caps dismissed_comment at 280 characters (422 above it, measured). 'short' is what gets POSTED; 'justifications' is the full reasoning and lives here, in version control, where it can be reviewed and corrected. The short text must name its group so the two can be tied together from the alert.",
  "short": {
    "registry-path-chain": "False positive: registry.json -> _entry_path (refuses non-dict, null, empty, and any '..' under both Windows and POSIX flavours) -> get_model_info(allow_direct_path=False) -> a read-only probe. Full reasoning: scripts/codeql/dispositions.json, group registry-path-chain.",
    "plugin-id-choke": "False positive: the id is validated by _is_valid_plugin_name wherever it becomes a path component (_installed_dir, _store_dir, and _swap_in_store_copy's staging assertion), so no separator, drive letter or '..' survives. See dispositions.json, group plugin-id-choke.",
    "inline-dominating": "False positive: the guard is inline in the sink's own function and dominates it (runs first on every path, no branch around it, raises). No cross-function traversal exists for a barrier to observe. See scripts/codeql/dispositions.json, group inline-dominating.",
    "choke-point": "False positive, structurally unclosable: this alert IS the confinement helper. The flagged expression is the path construction the containment test needs, so the guard can never precede its own sink. See scripts/codeql/dispositions.json, group choke-point.",
    "confined-under": "False positive: every flow passes pathsafe.confined_under (rejects empty, absolute, per-component drive-qualified, '..', and anything not strictly below base). Confirmed by declaring it a barrier offline. See scripts/codeql/dispositions.json, group confined-under.",
    "authorization-gated": "Won't fix: the destination is unconstrained because naming it IS the feature; authorization gates it, not confinement. Open mode is owner-equivalent but loopback-only (a bind past loopback without auth exits 2 unless --insecure). See dispositions.json: authorization-gated.",
    "operator-env": "Won't fix, accepted by threat model rather than closed by a barrier: LLAMA_CPP_LIB comes from localm's own environment and names the library to load; only the launching principal can set it, and it already runs code as that user. See dispositions.json, group operator-env.",
    "guarded-delete-model": "False positive: the delete is gated by is_owned_model_path(target), inline and dominating, on a path resolved once and used throughout; the path itself arrives via _entry_path. See scripts/codeql/dispositions.json, group guarded-delete-model.",
    "guarded-delete-plugin": "False positive: every use of d (containment test, is_dir/rmtree delete, outcome probes) is dominated by d.resolve().parent == root.resolve(), which raises unless d is a direct child of the installed root. See dispositions.json, guarded-delete-plugin.",
    "model-lookup-gate": "False positive: the registry branch routes through _entry_path; the direct-path branch needs allow_direct_path=True and every reported flow leaves it at its refusing default, checked inline. See scripts/codeql/dispositions.json, group model-lookup-gate.",
    "plugin-swap-invariant": "False positive: the id was validated at the _installed_dir / _store_dir choke point, and this function re-asserts containment inline (dest, tmp and backup all in the installed root), dominating its own sinks. See dispositions.json, group plugin-swap-invariant.",
    "confinement-fixed-2026-07-29": "Fixed 2026-07-29: the sink now consumes the value the guard returned. The alert persists because the flagged expression is the path construction the check necessarily follows, not because the flow is unguarded. See scripts/codeql/dispositions.json.",
    "weak-hash-generated-token": "False positive: the only human-chosen secret, the owner key, goes through scrypt and its record declares that construction. Everything else hashed here is a generated 256-bit token or a failed guess. See scripts/codeql/dispositions.json, group weak-hash-generated-token.",
    "dev-cli-argv": "Won't fix, accepted by threat model, not a barrier: this dev-only CLI script (release-excluded, never ships) reads the path its own invoker supplies via argv; only that same local principal can set it, already running as that OS user. See dispositions.json: dev-cli-argv."
  }
}
