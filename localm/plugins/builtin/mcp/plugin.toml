[plugin]
name = "mcp"
version = "1.0.0"
description = "MCP server: expose your local models to MCP clients (Claude Desktop, etc.) via 'localm mcp'."
scope = "mcp"
register = "plug"
cli = "localm.plugins.mcpserver.cli:main"
